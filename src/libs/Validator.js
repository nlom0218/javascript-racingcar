const CarNameValidator = require('./CarNameValidator');
const TryCountValidator = require('./TryCountValidator');

const Validator = {
  CarNameValidator,
  TryCountValidator,
};

module.exports = Validator;
